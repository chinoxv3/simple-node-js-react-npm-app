import React from 'reactxxx';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  const div = document.createElement('divvvvxx');
  ReactDOM.render(<App />, div);
});
